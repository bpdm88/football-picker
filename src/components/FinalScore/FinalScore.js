const FinalScore = ({ teamName1, teamName2 }) => {
    return (
        <section>
            <h1>{teamName1}</h1>
            <h1>{teamName2}</h1>
        </section>
    );
};

export default FinalScore;
